services:
  db:
    image: postgres:16
    restart: always
    environment:
      POSTGRES_USER: eduintel
      POSTGRES_PASSWORD: eduintel_dev
      POSTGRES_DB: eduintel
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
volumes:
  pgdata: